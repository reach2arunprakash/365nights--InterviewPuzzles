复制linked list。 已知每个节点有两个pointer，一个指向后一个节点，另一个指向
   其他任意一节点。 O(n)时间内，无附加内存，复制该linked list。（存储不连续）
copy a linked list. every node has two pointers, one to the next, one to another.

copy this link in O(n) without additional memory.